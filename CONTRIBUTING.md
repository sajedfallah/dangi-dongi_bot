# Contributing

1. Create a branch from `develop`.
2. Keep secrets out of commits.
3. Run `python -m compileall -q .` before pushing.
4. Add or update tests for behavior changes.
5. Use focused commits such as `fix:`, `feat:`, `docs:` and `test:`.
